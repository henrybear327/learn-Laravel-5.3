<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Note extends Model
{
    // $card = App\Card::first();
    // $note = new App\Note;
    // $note->body = 'Some note for the card';
    // $note->card_id = 2;
    // $note->save();
    // App\Note::all();
}
